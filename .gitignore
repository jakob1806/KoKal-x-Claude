# Env & Secrets
# Flutter / Dart
# Node (Admin-Dashboard)
# OS
# Supabase
*.key
*.pem
.DS_Store
.claude/settings.local.json
.env
.env.*
# Negations must come after the .env.* exclusion above to take effect —
# gitignore rules apply top-to-bottom, an earlier negation gets re-excluded
# by a later pattern.
!.env.example
!.env.local.example
.next/
app/**/*.freezed.dart
app/**/*.g.dart
app/.dart_tool/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/.packages
app/android/.gradle/
app/build/
app/ios/.symlinks/
app/ios/Pods/
backend/supabase/.branches/
backend/supabase/.temp/
dist/
node_modules/
supabase/.branches/
supabase/.temp/
