package de.klangradar.android.ui.theme

import androidx.compose.material3.Typography

/** Default system font (Roboto) — same choice ios-native made with the
 *  implicit SF Pro system font (no custom font family in DesignSystem). */
val KlangradarTypography = Typography()
